# 2026-07-17 Daily Notes

## Chrome Debug Port Fix (CRITICAL)
- Chrome Profile 3 has a `LOCK` file in `User Data/Profile 3/LOCK` that prevents `--remote-debugging-port` from working
- **DevToolsActivePort file** in `Profile 3/DevToolsActivePort` also prevents the port — must delete it too
- Must kill Chrome + wait 5 seconds + force kill remaining + remove LOCK + remove DevToolsActivePort before launching
- Must use `--user-data-dir` pointing to `User Data` (NOT `User Data/Profile 3`)
- Must also pass `--profile-directory=Profile 3` separately
- Without `--user-data-dir`, Chrome ignores `--remote-debugging-port` entirely
- `--remote-allow-origins=*` is also needed for Playwright CDP connection
- Use `127.0.0.1:9222` not `localhost:9222` (localhost resolves to IPv6 which fails)
- **Keep Chrome open for entire batch** — do NOT open/close per contact

## Messenger Typing Fix
- Messenger uses Lexical rich text editor: `div[role=textbox] > ... > <p class="xat24cr xdj266r"><br></p>`
- `locator.fill()` does NOT work with Lexical
- `locator.type()` does NOT work reliably
- `page.keyboard.type()` after clicking `div[role="textbox"]` WORKS
- Subprocess output encoding: must use `encoding='utf-8', errors='replace'` on Windows

## Gmail Scripts Fix
- All email scripts in `outreach/email/` had wrong `user_data_dir` path
- They pointed to `User Data/Profile 3` instead of `User Data`
- Missing `--profile-directory=Profile 3` flag
- Missing `--remote-allow-origins=*` flag
- All fixed: auto_send_all.py, SEND_ALL_VENUES.py, SEND_EMAILS_NOW.py, open_gmail_simple.py, open_gmail.py, open_chrome_simple.py

## Workspace Organization
- Reorganized workspace into clear directory structure
- `outreach/email/` - all email scripts
- `outreach/messenger/` - all messenger scripts (moved from fbfriends/)
- `outreach/venues/` - all venue CSV files
- `outreach/docs/` - outreach plans and docs
- `outreach/archive/` - old files, Facebook data dump, stage plot
- `docs/` - setup guides and summaries
# 2026-07-17 Daily Notes

## Chrome Debug Port Fix (CRITICAL)
- Chrome Profile 3 has a `LOCK` file in `User Data/Profile 3/LOCK` that prevents `--remote-debugging-port` from working
- **DevToolsActivePort file** in `Profile 3/DevToolsActivePort` also prevents the port — must delete it too
- **Current Session / Current Tabs files** must be deleted to prevent junk tabs (data/, 0.0.0.3)
- Must kill Chrome + wait 5 seconds + force kill remaining + remove LOCK + remove DevToolsActivePort before launching
- Must use `--user-data-dir` pointing to `User Data` (NOT `User Data/Profile 3`)
- Must also pass `--profile-directory=Profile 3` separately
- Without `--user-data-dir`, Chrome ignores `--remote-debugging-port` entirely
- `--remote-allow-origins=*` is also needed for Playwright CDP connection
- Use `127.0.0.1:9222` not `localhost:9222` (localhost resolves to IPv6 which fails)
- **Keep Chrome open for entire batch** — do NOT open/close per contact
- **PowerShell Start-Process** is the only reliable way to launch Chrome from Python (subprocess.Popen with list does NOT work)
- **Each argument must be a separate quoted item** in ArgumentList array, otherwise spaces in paths split into multiple URLs (causing data/ and 0.0.0.3 junk tabs)

## Messenger Typing Fix
- Messenger uses Lexical rich text editor: `div[role=textbox] > ... > <p class="xat24cr xdj266r"><br></p>`
- `locator.fill()` does NOT work with Lexical
- `locator.type()` does NOT work reliably
- `page.keyboard.type()` after clicking `div[role="textbox"]` WORKS
- Subprocess output encoding: must use `encoding='utf-8', errors='replace'` on Windows
- **Continue button**: Facebook shows a "Continue" or "Continue as" interstitial page. Must detect and click it (up to 5 retries with 2s gaps) before textbox appears.

## Gmail Scripts Fix
- All email scripts in `outreach/email/` had wrong `user_data_dir` path
- They pointed to `User Data/Profile 3` instead of `User Data`
- Missing `--profile-directory=Profile 3` flag
- Missing `--remote-allow-origins=*` flag
- All fixed and archived redundant scripts. Only SEND_ALL_VENUES.py + SEND_ALL.bat remain in outreach/email/

## Workspace Organization
- Reorganized workspace into clear directory structure
- `outreach/email/` - SEND_ALL_VENUES.py + SEND_ALL.bat (main email script)
- `outreach/messenger/` - messenger_terminal.py, auto_send.js, debug_loop.js, all messenger files
- `outreach/venues/` - all venue CSV files
- `outreach/docs/` - outreach plans and docs
- `outreach/archive/` - old files, Facebook data dump, stage plot, 14 redundant email scripts
- `docs/` - setup guides and summaries

## Duplicate Prevention (4 Layers)
1. CSV Check: Skip contacts where `message_sent` in ('true', 'bad')
2. History File: Check `message_history.json` for current event URL
3. Session Tracking: Track sends during current session
4. In-conversation scan: Check if event URL/ID already exists in conversation text before typing

## Bad Profile Marking
- Auto-mark as `bad` on: timeout, no textbox found, navigation error
- Manual mark with `b` key at prompt
- Bad profiles permanently skipped in load_contacts
- `mark_contact_bad()` sets message_sent='bad' and records error in last_error

## NY Location Filter
- `contacts/fb_friend.csv` has location data in column 4 (City, State)
- `load_ny_profiles()` reads this and builds a set of NY-area profile IDs
- NY keywords: new york, nyc, brooklyn, manhattan, queens, bronx, staten island, astoria, harlem, long island, hoboken, jersey city, yonkers, white plains, ', ny'
- `load_contacts(ny_only=True)` filters to only NY-area contacts

## Debug Loop
- `debug_loop.js` — reads CSV, navigates to first 10 pending contacts, takes screenshots, types but does NOT send
- Screenshots saved to `debug_screenshots/` folder: before_, after_click_, after_type_ for each contact
- Confirmed 12/18 profiles work (67% success rate)
- Bad profiles removed from CSV by user

## Auto Send Script
- `auto_send_100.js` — sends to 100 NY contacts automatically
- Reads contacts from `contacts_100.js` (generated by `gen_contacts.py`)
- Navigates → detects Continue button → waits for textbox → checks already sent → types → presses Enter → updates CSV + history
- Screenshots at each step
- 5-15s delay between messages
- First test batch: Frank Fois sent successfully, Allen/Eleanor/Gonzalo already sent, David Fleming bad profile

## Speed Optimizations (70% faster)
- Typing delay: 3ms/char (was 10ms)
- Focus wait: 500ms (was 200ms)
- Navigation: domcontentloaded (was networkidle which always timed out)
- Auto-send: Playwright presses Enter after typing (no manual review)
- No post-send ENTER wait: moves to next contact immediately
- Rate limit: 5-15s (was 30-120s) — only after successful sends
- Skip/bad/error: instant move to next contact (no waiting)

## GitHub Repo
- Repo: https://github.com/raymondbernard/suprfanz
- Committed and pushed: messenger terminal, email scripts, venue data, workspace structure

## Skill Updated
- Messenger skill at ~/.openclaw/workspace/skills/messenger/SKILL.md
- Updated 3 times throughout the day with all learnings
- Includes: Chrome debug port setup, Lexical typing, Continue button, NY filter, bad profiles, screenshots, speed optimizations