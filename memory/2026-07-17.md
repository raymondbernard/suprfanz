# 2026-07-17 Daily Notes

## Chrome Debug Port Fix (CRITICAL)
- Chrome Profile 3 has a `LOCK` file in `User Data/Profile 3/LOCK` that prevents `--remote-debugging-port` from working
- **DevToolsActivePort file** in `Profile 3/DevToolsActivePort` also prevents the port — must delete it too
- Must kill Chrome + wait 5 seconds + force kill remaining + remove LOCK + remove DevToolsActivePort before launching
- Must use `--user-data-dir` pointing to `User Data` (NOT `User Data/Profile 3`)
- Must also pass `--profile-directory=Profile 3` separately
- Without `--user-data-dir`, Chrome ignores `--remote-debugging-port` entirely
- `--remote-allow-origins=*` is also needed for Playwright CDP connection
- Use `127.0.0.1:9222` not `localhost:9222` (localhost resolves to IPv6 which fails)
- **Keep Chrome open for entire batch** — do NOT open/close per contact

## Messenger Typing Fix
- Messenger uses Lexical rich text editor: `div[role=textbox] > ... > <p class="xat24cr xdj266r"><br></p>`
- `locator.fill()` does NOT work with Lexical
- `locator.type()` does NOT work reliably
- `page.keyboard.type()` after clicking `div[role="textbox"]` WORKS
- Subprocess output encoding: must use `encoding='utf-8', errors='replace'` on Windows

## Gmail Scripts Fix
- All email scripts in `outreach/email/` had wrong `user_data_dir` path
- They pointed to `User Data/Profile 3` instead of `User Data`
- Missing `--profile-directory=Profile 3` flag
- Missing `--remote-allow-origins=*` flag
- All fixed: auto_send_all.py, SEND_ALL_VENUES.py, SEND_EMAILS_NOW.py, open_gmail_simple.py, open_gmail.py, open_chrome_simple.py

## Workspace Organization
- Reorganized workspace into clear directory structure
- `outreach/email/` - all email scripts
- `outreach/messenger/` - all messenger scripts (moved from fbfriends/)
- `outreach/venues/` - all venue CSV files
- `outreach/docs/` - outreach plans and docs
- `outreach/archive/` - old files, Facebook data dump, stage plot
- `docs/` - setup guides and summaries